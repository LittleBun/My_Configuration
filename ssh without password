做过好几次了，每次都得上网搜步骤，这次记下来。
在本地用ssh-keygen -t rsa 生成rsa密钥对。
确认远程机器存在目录 ~/.ssh，并且权限是700。
确认远程机器存在文件~/.ssh/authorized_keys，并且权限是600。
将本地的rsa公钥id_rsa.pub的内容附加到远程文件~/.ssh/authorized_keys的末尾。
使用ssh name@host " cmd string"来执行远程命令。
